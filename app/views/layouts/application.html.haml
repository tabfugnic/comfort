!!!
%html
  %head
    %title Comfort
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    #header
      %h1= link_to "Comfort", "/", {id: "logo"}
      %ul
        - unless user_signed_in?
          %li= link_to "Sign In", "/users/sign_in"
        - else
          %li= link_to current_user.email, user_path(current_user)
          %li= link_to "Sign Out", "/users/sign_out"
    #alert= flash[:alert]
    #error= flash[:error]
    #notice= flash[:notice]
    #main
      = yield
    #secondary
      = yield(:secondary)
